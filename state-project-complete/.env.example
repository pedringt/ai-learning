# Copy this file to .env (already gitignored -- never commit the real one)
# and fill in a real key to run the real-provider eval/test suites locally,
# e.g. `python3 -m eval.run_eval` from this directory.
ANTHROPIC_API_KEY=
