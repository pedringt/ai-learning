STATE Render/Neon Repair R2
Build marker: neon-repair-2026-09-02-r2

IMPORTANT: This zip is intentionally FLAT. It does NOT contain an outer
state-project-complete folder.

GitHub upload:
1. Open your repository on GitHub.
2. Click into the EXISTING folder: state-project-complete
3. Upload/replace the files from THIS folder there.
4. Also replace migrations/001_initial.sql with the included version.
5. Commit directly to the SAME BRANCH that Render deploys.
6. In GitHub, open state-project-complete/api.py and verify you can find:
      STATE_BUILD_REV = "neon-repair-2026-09-02-r2"
   and CANNOT find:
      connection.execute = execute
7. Redeploy on Render.

Expected Render log if the correct source is being deployed:
  [STATE] Starting build neon-repair-2026-09-02-r2

If that line does not appear, stop changing Python code: Render is deploying a
different branch/repository/root directory than the GitHub location you edited.

Local verification completed before packaging:
  127 passed, 3 skipped, 7 subtests passed
  FastAPI lifespan startup succeeded
  GET /health -> 200 {"status":"ok"}
