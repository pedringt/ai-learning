# State product risk register

Keep this focused on material product/AI risks, not every possible defect. Link actionable mitigation work to GitHub Issues.

State is a portfolio/learning product. Some risks below deliberately model what a real company product would need to manage. Do not interpret those hypothetical production risks as evidence of real customers or an external rollout.

| ID | Risk | Severity | Current mitigation | Status / trigger |
| --- | --- | --- | --- | --- |
| R-001 | AI presents unsupported or fabricated information as established project truth | P1 | Human authorization boundary; Ask grounding rules; real-model evals; regression tests for known failures | Active. Treat provenance/certainty failures as release-relevant. |
| R-002 | Unknown or ambiguous evidence is converted into false certainty (for example unknown -> zero/false/absent) | P1 | Semantic validation, eval cases, human review for consequential ambiguity | Active. Add regression/eval case for every reproduced variant. |
| R-003 | Cross-project data or writes leak between projects | P0 | Explicit project identity on requests; isolation tests; deployed QA | Active. Any recurrence blocks release. |
| R-004 | State creates too many Reviews and people stop treating them as meaningful | P1 | Consequentiality rules; review-burden eval dimension; selective escalation | Active product risk. Evaluate with representative scenarios and review samples rather than pretending real-user burden data exists. |
| R-005 | State misses a consequential change that should have reached human review | P1 | Real-model consequentiality evals plus deterministic enforcement where possible | Active. False negatives matter more than small precision loss. |
| R-006 | Stale proposals overwrite newer project truth | P0 | Version checks / stale-review blocking / atomic transitions | Mitigated by deterministic controls; keep regression coverage. |
| R-007 | Ask confuses pending Reviews/open Questions with Current State | P1 | Grounding rules, record-backed retrieval/navigation, evals | Active. Treat confident misrepresentation as a release blocker. |
| R-008 | Real-model QA becomes expensive/noisy and gets skipped or overused | P2 | `qa-fast` vs `qa-release`; model-sensitive diff gating; targeted live-model checks | Managed. Prefer cheaper deterministic layers first. |
| R-009 | Project-area identifiers are not truly project-scoped, so a hand-chosen area id could collide across projects | P2 (downgraded from P1 on 2026-09-19) | User-created projects generate area ids as `area_<uuid>` (`baseline_setup.py`, `baseline_draft.py`), so they cannot collide today; only hand-seeded areas (`seed_demo.py`) use human-chosen ids. Tracked as #136. **Since 2026-09-20 (option C, no schema change):** seeding a project whose area id is already taken **raises** instead of silently skipping, and tests pin that hand-seeded ids are unique across seeded projects and that user projects generate `area_<uuid>` ids, so the hazard cannot be hit silently. See `docs/architecture/PROPOSAL_136_PROJECT_SCOPED_AREA_IDS.md`. | Deferred (the structural limit remains). Resolve before adding another hand-seeded project or any path that lets a person or model choose an area id. The fix is a schema migration that needs real Postgres coverage, and staging has none (R-014). |
| R-010 | Shared staging data accumulates test Evidence and influences Ask/evals | P2 | Explicit clean-baseline decisions; avoid unapproved resets; distinguish deterministic from live staging tests | Active operational risk. |
| R-011 | A real deployment could send confidential, personal, or otherwise sensitive source content to a model/provider, trace, or log without an explicit data-handling decision | P1 | Portfolio/demo data boundary; `DATA_PRIVACY.md`; PR data-impact check; least-privilege/source-authorization requirements for any hypothetical real connector | Hypothetical production risk. Becomes active implementation risk before any real company source is connected. |
| R-012 | Model latency or per-task cost makes core workflows impractical even when output quality is acceptable | P2 | Measure representative Evidence/Ask latency and provider cost; keep real-model checks targeted; evaluate quality/cost/speed together | Active AI-product tradeoff. Use measured test/eval runs, not invented user metrics. |
| R-013 | A provider/model/prompt/config change shifts semantic behavior without an obvious software regression | P1 | Pin/record model configuration where practical; model-sensitive PR checklist; targeted evals before consequential release; rollback plan | Active. Re-evaluate known behavior families after meaningful model or prompt changes. |
| R-014 | `state-api-staging` has no persistent disk and no Postgres, so every staging deploy silently wipes non-seeded project data | P2 | Accepted staging tradeoff (free tier); production analog tracked as #145 | Active operational risk on staging. Confirmed in practice 2026-09-15: two user-created staging projects' IDs changed mid-session across redeploys, discarding their Notes/Current State. Expect this whenever staging is used to dogfood across more than one deploy. |
| R-015 | Product analytics metadata or a cross-project admin dashboard could reveal more about project activity than the viewer is authorized to know, even without showing project content | P1 before real deployment | Metadata-only allowlists; no project-content fields; no direct Open State affordance; short usage-event retention; portfolio/demo-data boundary | Portfolio implementation risk only. Before any real customer/company data, add authenticated admin RBAC, tenant scoping, deletion behavior, and authorization tests for analytics endpoints. |
| R-016 | Baseline Setup analysis can fail or leave the user with an empty draft: the model occasionally returns output that fails schema validation, extracts fewer facts/areas than a person would expect, or the review dialog opened during analysis may not redraw when it finishes | P1 (reliability of a first-run flow) | Failed interpretations are recorded and a banner tells the person; nothing becomes Current State without human confirmation; Deep QA exercises the flow against the real deployed stack | Partly mitigated, tracked as #230 (fix on `staging` at `7ef2837`). Seen 4 of 4 Deep QA runs on 2026-09-19 with 4 different symptoms; not caused by the #228 promotion diff. Root cause of two symptoms was in the app: the review dialog was a one-time snapshot (opened while analysis ran, it never redrew when analysis finished), and the manual-add path reopened the dialog by clicking a banner button that is absent mid-render. Both fixed with real-browser regression tests that fail on the old code; Deep QA then passed 4 of 4 on the fixed revision. Deep QA now gates only on app correctness and records model quality as observations (DEC-008); it passed 4 of 4 on `502b097`. **Still open:** the model can still return schema-violating output, put every fact in one area, or raise a Review that blocks Confirm (the test reports that as skipped, inconclusive); a failed analysis now offers a manual retry from the Baseline UI (#231, live on production since PR #236) but nothing retries automatically (decided Sept 20, DEC-009: no automatic retry for now, revisit on the triggers recorded there; the first valid measurements were 1 failed analysis in 36 real-model runs, cause not identified and not reproduced in 12 targeted re-runs, see `docs/evals/README.md`); Evidence added right after a page load can go to the seed project (#232); and no eval watches Baseline decomposition quality (#233). |
| R-017 | The Vercel free plan allows 100 deployments per day for the whole team (skipped builds appear to count, and Tastemake shares it), so a burst of pushes can leave a release half deployed: on Sept 20-21 the Render backend and the portfolio deployed but the `state` frontend was refused ("Deployment rate limited", API 402 `api-deployments-free-per-day`) | P2 (portfolio site; the only visible effect was a stale frontend) | Batch pushes; the `state` Ignored Build Step now skips when the app folder is unchanged since the last deployed commit; after any promotion check the `Vercel – state` status and the served code; never promote a preview deployment (its `api/state-config.js` points at the staging API) | Accepted while the site is frozen (few deploys). The quoted reset time was conservative: the cap lifted earlier on Sept 21. |
| R-018 | Production Render `state-api` has no health-check path, so deploys are not zero-downtime: requests during the roughly 90-second swap get 502 (a test session hit it on Sept 21 during PR #250's deploy) | P2 | Deploys are short and infrequent; `scripts/verify-render-deploy.sh` checks the served build | Optional dashboard fix: Settings, Health Check Path `/health`. Paige chose to wait (Sept 21); revisit if 502s cause more problems. |
| R-019 | The live production demo data is writable by anyone (no auth), so the showcase can drift from the seed: on Sept 21 a test session added Evidence and accepted a Review on Northstar | P2 | The app's Reset restores a seeded project; the Sept 20 baseline counts are recorded in `docs/PROJECT_STATUS.md`; Render keeps daily disk snapshots | Reset restored it on Sept 21 (with Paige's OK). Accepted for a portfolio demo; revisit if a real project ever lives on production. |

## How to use this file

- Add a risk when it can materially affect trust, authority, user burden, data isolation, privacy, cost, latency, or release confidence.
- Open an Issue when mitigation requires work; do not use this table as the implementation backlog.
- Close/remove risks only when the underlying condition is genuinely gone. If merely accepted or deferred, say so.
- After a P0/P1 incident, check whether this register needs a new risk or stronger mitigation.
- Keep hypothetical enterprise risks clearly labeled until the product actually has the data/users that make them real operational risks.
